{{--
    Текст одной статьи справки.

    `fi-prose` — штатный класс темы Filament со всей типографикой готового
    HTML: заголовки, списки, таблицы, код, цитаты и тёмная тема. Своих
    стилей здесь нет и быть не должно; ограничения ширины утилитой Tailwind
    тоже нет — генерических утилит в вендорной теме панели не собрано,
    и класс вроде `max-w-3xl` не дал бы ни строки CSS, зато выглядел бы
    в разметке работающим.

    `{!! !!}` безопасен: HTML пришёл из `HelpContent::render()`, где
    конвертер запущен с `html_input => strip` и `allow_unsafe_links => false`.
--}}

@php
    use App\Filament\Pages\Help\Article;
@endphp

<x-filament-panels::page>
    <x-filament::section>
        <div class="fi-prose">
            {!! $body !!}
        </div>
    </x-filament::section>

    @if ($related !== [])
        {{--
            Перекрёстные связи объявлены в реестре и напечатаны здесь,
            а не проставлены ссылками внутри текста: переименованный слаг
            обязан ронять тест, а не страницу. Список уже отфильтрован
            по видимости — закрытых заголовков в нём нет.
        --}}
        <x-filament::section heading="См. также">
            {{--
                Список тоже под `fi-prose`, а не с компонентом-ссылкой
                внутри голого списка: маркеры и отступы `<ul>` берутся
                из той же типографики, что и списки внутри самой статьи.
                Иначе блок «См. также» выглядит иначе, чем текст над ним,
                — при том, что это тот же самый список ссылок.
            --}}
            <div class="fi-prose">
                <ul>
                    @foreach ($related as $article)
                        <li>
                            <a href="{{ Article::getUrl(['article' => $article->slug]) }}">
                                {{ $article->title }}
                            </a>
                        </li>
                    @endforeach
                </ul>
            </div>
        </x-filament::section>
    @endif
</x-filament-panels::page>
