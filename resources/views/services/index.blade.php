{{--
    Автосервис (веха 4.4), свёрстан по `assets/mockup-v2/services.html`.

    Данные собирает `App\Services\ServicesPageContent`: блоки прайса
    по категориям, описания категорий, оговорка о ценах и блок «почему сюда».
    Блоки, управляемые данными, при пустом значении не рендерятся вовсе —
    заголовок «Шиномонтаж» над пустым местом читается как поломка, а не
    как «мало контента».

    Три вещи из макета сюда НЕ переносятся, и каждая — не мелочь:

    - состав меню шапки («Продажа автомобилей · Запчасти · Сервис
      и детейлинг»). Навигация живёт в `App\Support\SiteMenu`, у нас
      `/catalog` и `/parts` — отдельные страницы, а не якоря главной.
      Макет v2 нарисован для одностраничника, и его структура просачивается
      по частям: сначала подпись пункта, потом ссылка. Сторож — `LayoutTest`;
    - заголовок «Сервис и детейлинг». H1 приходит из настройки
      `services_page.intro_title` (веха 3.5), и заменить её константой
      из макета значит отобрать у заказчика поле, которое ему уже отдали.
      Сторож — `SectionPagesTest`;
    - акцентное слово в H1 (`<em>детейлинг</em>`). Настройка хранит текст,
      а не разметку: значение печатается экранированным (иначе это XSS
      через админку), а редактор не поймёт, зачем в поле угловые скобки.
      H1 этой страницы одноцветный — единственное осознанное расхождение
      с макетом;
    - иконки в кружке у заголовков категорий. Сняты по просьбе заказчика
      19.08.2026 вместе с вехой 4.13, и снятие сквозное: те же категории
      набирались тем же значком на витрине главной, и оставить их там
      значило бы развести два вида одного заголовка. Метод
      `ServiceCategory::icon()` УДАЛЁН вместе с самим енамом, а не оставлен
      неиспользуемым: мёртвый метод в справочнике читается как «иконку
      просто забыли вывести».

    Правка любого из двух названных тестов «чтобы совпало с макетом» —
    сигнал, что веха вышла за свои границы.

    Позиции внутри блока категории идут ТРЕМЯ группами (веха 4.13):
    акцентные широкие карточки, карточки с фотографией, строки прайса.
    Группы разделяет `ServicesPageContent`, а не шаблон: три `@if`
    на карточку в трёх местах разъехались бы при первой правке одного
    из них. Порядок внутри группы задаёт `sort_order`.
--}}
@extends('layouts.app')

@section('title', $title.' — '.config('app.name'))
@section('description', 'ТО и ремонт, шиномонтаж, детейлинг и дополнительные сервисы для автомобилей любых марок.')

@section('content')
    {{-- Страница автосервиса светлая целиком (веха 4.11): шапка, прайс
         и «почему сюда» — три светлые секции подряд, из них последняя
         на тон темнее (`page-alt`). Секцию заявки под ними красит сам
         компонент по своей раскладке. --}}
    <x-page-heading eyebrow="Услуги" :title="$title" :intro="$intro" class="theme-light">
        {{-- Якорная навигация: прайс длинный, и человек, пришедший
             за шиномонтажом, не должен пролистывать три чужие категории.

             Ссылки строятся из того же `$blocks`, что и сами блоки, а не
             из полного списка категорий: ссылка на несуществующий якорь
             не работает МОЛЧА. При одном блоке навигации нет вовсе —
             листать нечего. --}}
        @if (count($blocks) > 1)
            <x-slot:below>
                <nav class="flex flex-wrap gap-2.5" aria-label="Категории услуг">
                    @foreach ($blocks as $block)
                        <a
                            href="#{{ $block['anchor'] }}"
                            class="rounded-full border border-line-strong px-5 py-2.5 text-sm transition-colors hover:border-accent/50 hover:text-accent"
                        >{{ $block['category']->name }}</a>
                    @endforeach
                </nav>
            </x-slot:below>
        @endif
    </x-page-heading>

    {{--
        Обёртка Alpine охватывает и прайс, и секцию заявки, — иначе обработчик
        на строке прайса окажется вне области компонента и не сработает вовсе:
        область `x-data` — это поддерево DOM, а не «страница». Две соседние
        обёртки здесь не годятся по той же причине.

        Компонент делает ровно одно: подставляет выбранную позицию в селект
        формы. Строки прайса остаются обычными ссылками `href="#lead-form"`,
        поэтому без скрипта клик доводит до формы, и посетитель выбирает
        позицию в списке сам. `x-data` инлайном, а не через `Alpine.data()`
        из `@push('scripts')` — правило `RULES.md`, выведенное вехой 4.3.
    --}}
    <div
        x-data="{
            pick(id) {
                const select = document.getElementById('lead-service');

                if (select !== null) {
                    select.value = id;
                }
            },
        }"
    >
        @if ($blocks !== [])
            {{-- Светлая тема висит на обёртке с боковыми полями, а не
                 на секциях внутри неё: у секций полей нет, они лежат
                 в колонке контента, и фон каждой оборвался бы по её
                 краям — по светлой странице пошли бы тёмные полосы
                 слева и справа.

                 Нижний отступ — `pb` здесь, а не `mb` у оговорки о ценах,
                 и это не стилистика. Вертикальный внешний отступ последнего
                 потомка схлопывается наружу через обёртки без padding
                 и границы, то есть выпадает ИЗ закрашенной области: `mb-6`
                 у оговорки давал ровно 24 пикселя тёмной полосы поперёк
                 светлой страницы. До вехи 4.11 этого не было видно, потому
                 что по обе стороны полосы был один цвет. --}}
            <div class="theme-light px-5 pb-6 lg:px-8">
                <div class="mx-auto max-w-page">
                    @foreach ($blocks as $index => $block)
                        {{-- Верхняя граница разделяет блоки, поэтому у первого
                             её нет: иначе она удвоила бы границу шапки
                             страницы. --}}
                        <section
                            id="{{ $block['anchor'] }}"
                            @class([
                                'py-13 lg:py-16',
                                'border-t border-line' => $index > 0,
                            ])
                        >
                            {{-- Шапка блока занимает ВСЮ ширину контента,
                                 а не левую колонку сетки, — и это следствие,
                                 а не вкус. Акцентная карточка обязана
                                 занимать всю ширину экрана контента
                                 (постановка 19.08.2026), а внутри колонки
                                 `lg:grid-cols-[0.8fr_1.2fr]` она шире
                                 колонки не станет. Прежняя сетка «описание
                                 слева, прайс справа» вместе с ней ушла;
                                 прайс сохранил свои две колонки.

                                 Описание ограничено `max-w-2xl`: строка
                                 на всю ширину `max-w-page` нечитаема. --}}
                            <div class="mb-9 lg:mb-11">
                                <h2 class="font-display text-[26px] leading-[1.2] font-semibold lg:text-3xl">
                                    {{ $block['category']->name }}
                                </h2>

                                {{-- Пустое описание убирает абзац,
                                     но не блок: прайс важнее текста,
                                     и категория без описания остаётся
                                     на странице. --}}
                                @if ($block['note'] !== null)
                                    <p class="mt-3.5 max-w-2xl text-sm leading-[1.7] text-ink-muted">{{ $block['note'] }}</p>
                                @endif
                            </div>

                            {{-- Три группы стопкой сверху вниз, каждая своей
                                 раскладкой. Зазор задаётся `gap` на обёртке,
                                 а не отступами у групп: пустая группа
                                 не рендерится вовсе, и `gap` между
                                 отсутствующими соседями не появляется —
                                 в отличие от `mt` у каждой. --}}
                            <div class="flex flex-col gap-10 lg:gap-12">
                                {{--
                                    ГРУППА 1 — акцентные позиции: широкая
                                    карточка во всю ширину контента.

                                    Механику повторяет полоса доверия главной
                                    (кадр подложкой, две маски `from-scrim/85`,
                                    содержимое в левом верхнем углу и внизу,
                                    кадр в поток ниже `lg`), но общий компонент
                                    из них НЕ выносится: совпадают приёмы,
                                    а не содержимое. У полосы доверия внизу
                                    три стеклянные карточки, здесь — описание
                                    и цена одной услуги; там кадр несёт смысл
                                    сам («мы существуем»), здесь он
                                    ИЛЛЮСТРИРУЕТ услугу, и `alt` тоже
                                    настоящий. Компонент с четырьмя слотами
                                    и тремя флагами читается хуже двух
                                    шаблонов.

                                    ЖЁЛТОГО ТЕКСТА ПОВЕРХ КАДРА ЗДЕСЬ НЕТ,
                                    и это не недосмотр. Заказчик просил
                                    «акцентное название», буквальное прочтение
                                    — `text-accent`, и оно запрещено
                                    `docs/design-system.md`: токен акцента
                                    с темой не переключается и даёт 1.4:1
                                    на светлом кадре. Акцент здесь
                                    типографский (крупный `font-display`
                                    чернилами `text-on-photo` против мелкой
                                    разрядки категории над ним) плюс СПЛОШНАЯ
                                    заливка `bg-accent-solid` у плашки цены,
                                    где контраст обеспечен внутри пары
                                    `accent-solid`/`on-accent`, а не темой
                                    вокруг.
                                --}}
                                @foreach ($block['featured'] as $service)
                                    <div x-data="{ open: false }">
                                        {{-- Карточка кликабельна целиком, как
                                             строка прайса: обычная ссылка
                                             на форму, поэтому без скрипта клик
                                             доводит до формы. Кнопка
                                             «Подробнее» лежит ПОД ссылкой,
                                             а не внутри неё: интерактивный
                                             элемент внутри `<a>` — невалидная
                                             разметка, и клавиатурный фокус
                                             в ней разъезжается. --}}
                                        <a
                                            {{-- Метка для сторожа палитры:
                                                 утверждения про акцентную
                                                 карточку («нет `text-accent`»,
                                                 «нет `text-on-photo` без
                                                 кадра») неверны для остальной
                                                 страницы, и тест обязан
                                                 вырезать именно её. Цепляться
                                                 за класс раскладки значит
                                                 сломать тест первой же
                                                 правкой вёрстки — прецедент
                                                 `data-server-error`
                                                 в форме заявки. --}}
                                            data-featured
                                            href="#lead-form"
                                            x-on:click="pick({{ $service->getKey() }})"
                                            @class([
                                                'group relative flex flex-col overflow-hidden rounded-card',
                                                // Без фотографии пары
                                                // `scrim`/`on-photo` нет —
                                                // ей не на чем работать,
                                                // и `text-on-photo` на светлой
                                                // карточке был бы невидим.
                                                // Карточка остаётся широкой
                                                // и остаётся акцентной:
                                                // администратор ставит флаг
                                                // раньше, чем получает кадр.
                                                'border border-line bg-surface transition duration-250 hover:border-accent/30' => $service->media_id === null,
                                                'lg:min-h-120 lg:justify-end' => $service->media_id !== null,
                                            ])
                                        >
                                            {{-- Кадр вместе с масками и шапкой
                                                 — своя система координат, как
                                                 у полосы доверия. Ниже `lg`
                                                 это обычный блок в потоке,
                                                 от `lg` он растягивается
                                                 подложкой карточки, а шапка
                                                 в обоих случаях позиционируется
                                                 относительно САМОГО кадра:
                                                 иначе ниже `lg` она уехала бы
                                                 на описание под фотографией,
                                                 а верхняя маска накрыла бы
                                                 три пятых всей карточки
                                                 вместо трёх пятых снимка. --}}
                                            <div @class([
                                                'relative',
                                                'overflow-hidden rounded-card lg:absolute lg:inset-0 lg:rounded-none' => $service->media_id !== null,
                                            ])>
                                            @if ($service->imageUrl !== null)
                                                {{-- Кадр иллюстрирует услугу,
                                                     поэтому `alt` настоящий,
                                                     а не пустой с
                                                     `aria-hidden`.

                                                     `srcset` не задаётся:
                                                     `ImageProcessor`
                                                     ограничивает ширину
                                                     сверху, но не апскейлит,
                                                     и настоящих ширин
                                                     вариантов у записи
                                                     библиотеки нет — правило
                                                     `RULES.md` про
                                                     дескрипторы, которым
                                                     нечем подтвердиться.

                                                     Пропорция растёт к узким
                                                     экранам (`5/4` против
                                                     `21/9`): шапка карточки
                                                     занимает верх кадра,
                                                     и на 390px при широкой
                                                     пропорции от снимка
                                                     остаётся полоска. --}}
                                                <img
                                                    src="{{ $service->imageUrl }}"
                                                    loading="lazy"
                                                    alt="{{ $service->title }}"
                                                    class="aspect-[5/4] w-full object-cover transition duration-500 group-hover:scale-[1.02] sm:aspect-[21/9] lg:absolute lg:inset-0 lg:size-full"
                                                >

                                                {{-- Масок две, как у полосы
                                                     доверия: верхняя под
                                                     шапку, нижняя под
                                                     описание с ценой. Общий
                                                     токен `--color-scrim`,
                                                     общее правило: подложка
                                                     под светлым содержимым
                                                     обязана быть тёмной.
                                                     Затемнение нужно и тогда,
                                                     когда конкретно этот
                                                     снимок в нём не
                                                     нуждается: фотографию
                                                     выбирает заказчик,
                                                     и завтра тёмный кадр
                                                     сменится светлым. --}}
                                                <div
                                                    aria-hidden="true"
                                                    class="pointer-events-none absolute inset-x-0 top-0 h-3/5 bg-gradient-to-b from-scrim/85 to-transparent"
                                                ></div>

                                                {{-- Нижняя маска нужна только
                                                     там, где под ней лежит
                                                     содержимое, то есть
                                                     от `lg`. Ниже описание
                                                     с ценой стоит ПОД кадром
                                                     в потоке, и затемнять
                                                     ради него низ снимка
                                                     значило бы портить кадр
                                                     без причины. --}}
                                                <div
                                                    aria-hidden="true"
                                                    class="pointer-events-none absolute inset-x-0 bottom-0 hidden h-3/5 bg-gradient-to-t from-scrim/85 to-transparent lg:block"
                                                ></div>
                                            @endif

                                            {{-- Шапка в левом верхнем углу:
                                                 название категории мелкой
                                                 разрядкой, под ним название
                                                 услуги крупно. «Не такое
                                                 акцентное» название категории
                                                 — в точности постановка
                                                 заказчика. --}}
                                            <div @class([
                                                    'absolute inset-x-0 top-0 p-6 lg:p-9' => $service->media_id !== null,
                                                    'p-8 pb-0' => $service->media_id === null,
                                                ])>
                                                    <div @class([
                                                        'text-[13px] tracking-[0.2em] uppercase',
                                                        'text-on-photo/70' => $service->media_id !== null,
                                                        'text-ink-muted' => $service->media_id === null,
                                                    ])>{{ $block['category']->name }}</div>

                                                    <h3 @class([
                                                        'mt-3 max-w-2xl font-display text-[26px] leading-[1.15] font-semibold text-balance lg:text-[34px]',
                                                        'text-on-photo' => $service->media_id !== null,
                                                        'text-ink' => $service->media_id === null,
                                                    ])>{{ $service->title }}</h3>
                                                </div>
                                            </div>

                                            <div @class([
                                                'relative flex flex-wrap items-center justify-between gap-5',
                                                'mt-5 lg:mt-0 lg:p-9' => $service->media_id !== null,
                                                'p-8 pt-6' => $service->media_id === null,
                                            ])>
                                                @if ($service->description !== null)
                                                    {{-- Чернила поверх фотографии — ТОЛЬКО от `lg`,
                                                         и это не украшение брейкпойнтом. Ниже `lg`
                                                         описание с ценой лежит ПОД кадром, в потоке,
                                                         на светлом фоне страницы: `text-on-photo`
                                                         там белым по белому — текст занимает место
                                                         и не виден. Сборка при этом зелёная,
                                                         разметка на месте; поймано глазами
                                                         на приёмке при 390px. --}}
                                                    <p @class([
                                                        'min-w-64 flex-1 text-sm leading-[1.65] text-ink-muted',
                                                        'lg:text-on-photo/75' => $service->media_id !== null,
                                                    ])>{{ $service->description }}</p>
                                                @endif

                                                {{-- Плашка цены — единственный
                                                     акцентный ЦВЕТ на кадре
                                                     (сплошная заливка, пара
                                                     `accent-solid`/`on-accent`
                                                     держит контраст сама).

                                                     «По запросу» плашку
                                                     получает нейтральную:
                                                     это не цена, и набирать
                                                     её акцентом наравне
                                                     с суммой значит обещать
                                                     то, чего в карточке нет.
                                                     Развилка идёт по
                                                     `hasPrice()`, а не по
                                                     сравнению вывода
                                                     со строкой «по запросу»,
                                                     — иначе формулировка жила
                                                     бы в двух местах. --}}
                                                <span @class([
                                                    'shrink-0 rounded-full px-5 py-2.5 font-display text-[15px] font-semibold whitespace-nowrap',
                                                    // Заливка акцента и её чернила — пара, которая
                                                    // с темой не переключается: она одинаково
                                                    // читается и на кадре, и на фоне страницы,
                                                    // поэтому брейкпойнт ей не нужен.
                                                    'bg-accent-solid text-on-accent' => $service->hasPrice(),
                                                    'border border-line bg-page-alt text-ink-muted' => ! $service->hasPrice(),
                                                    // А нейтральная плашка — нужен: ниже `lg` она
                                                    // стоит на фоне страницы, а не на кадре.
                                                    'lg:border-0 lg:bg-on-photo/12 lg:text-on-photo' => ! $service->hasPrice() && $service->media_id !== null,
                                                ])>{{ $service->priceLabel() }}</span>
                                            </div>
                                        </a>

                                        <x-services.details :service="$service" />
                                    </div>
                                @endforeach

                                {{-- ГРУППА 2 — позиции с фотографией: сетка
                                     карточек. Разметка карточки общая
                                     с посадочной запчастей (компонент),
                                     потому что два по-разному набранных вида
                                     одной карточки разъедутся на первой же
                                     правке одной из них. --}}
                                @if ($block['withPhoto']->isNotEmpty())
                                    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                                        @foreach ($block['withPhoto'] as $service)
                                            <x-services.card :service="$service" :pick="true" />
                                        @endforeach
                                    </div>
                                @endif

                                {{-- ГРУППА 3 — прайс в две колонки: позиции короткие,
                                     и в одну колонку страница превращается
                                     в узкую ленту с огромным пустым правым
                                     полем. --}}
                                @if ($block['plain']->isNotEmpty())
                                <div class="grid md:grid-cols-2 md:gap-x-10">
                                    @foreach ($block['plain'] as $service)
                                        {{-- Строка — обычная ссылка на форму,
                                             а не кнопка: без скрипта клик
                                             доводит до формы, и посетитель
                                             выбирает позицию сам. Alpine лишь
                                             подставляет её в селект, поэтому
                                             `x-on:click` идёт без `.prevent` —
                                             переход к форме нужен и с ним.

                                             Полная форма `x-on:click`, а не
                                             `@click`: `@` — префикс директив
                                             Blade (правило
                                             `ARCHITECTURE.md`).

                                             Верхняя граница гасится у первой
                                             строки КАЖДОЙ колонки, а не только
                                             у первой строки сетки. Разметкой
                                             это не решается: число позиций
                                             в категории меняется в админке. --}}
                                        <a
                                            href="#lead-form"
                                            x-on:click="pick({{ $service->getKey() }})"
                                            class="group flex items-baseline justify-between gap-4 border-t border-line py-3.5 first:border-t-0 md:[&:nth-child(2)]:border-t-0"
                                        >
                                            <span class="text-[15px] leading-[1.4] transition-colors group-hover:text-accent">{{ $service->title }}</span>

                                            {{-- «По запросу» — не цена,
                                                 и набирать её акцентом наравне
                                                 с суммой значит обещать то,
                                                 чего в строке нет. Развилка
                                                 идёт по `hasPrice()`, а не
                                                 по сравнению вывода со строкой
                                                 «по запросу»: иначе
                                                 формулировка жила бы
                                                 в двух местах. --}}
                                            <span
                                                @class([
                                                    'shrink-0 whitespace-nowrap text-sm',
                                                    'font-display font-semibold text-accent' => $service->hasPrice(),
                                                    'font-medium text-ink-muted' => ! $service->hasPrice(),
                                                ])
                                            >{{ $service->priceLabel() }}</span>
                                        </a>
                                    @endforeach
                                </div>
                                @endif
                            </div>
                        </section>
                    @endforeach

                    {{-- Оговорка о ценах. Текст юридически значимый и живёт
                         в настройках; пустое значение убирает блок ЦЕЛИКОМ —
                         вместе с плашкой, потому что плашка без пояснения
                         ничего не сообщает. --}}
                    @if ($disclaimer !== null)
                        <div class="flex flex-wrap items-center gap-4 rounded-card border border-accent/25 bg-accent-solid/6 px-6.5 py-5.5">
                            <span class="inline-block shrink-0 rounded-full border border-accent/40 px-2.5 py-1 text-[11px] tracking-[0.08em] text-accent uppercase">
                                Не публичная оферта
                            </span>

                            <p class="min-w-64 flex-1 text-sm leading-[1.65] text-ink-muted">{{ $disclaimer }}</p>
                        </div>
                    @endif
                </div>
            </div>
        @endif

        {{--
            «Почему сюда» — репитер `services_page.advantages`. Пустой список
            убирает секцию ЦЕЛИКОМ, вместе с фотопанелью: заголовок над пустой
            сеткой читается как поломка, а фотография без карточек рядом —
            как обрезанный блок.
        --}}
        @if ($advantages !== [])
            <section class="theme-light bg-page-alt px-5 py-20 lg:px-8 lg:py-30">
                <div class="mx-auto grid max-w-page items-start gap-8 lg:grid-cols-[0.85fr_1.15fr] lg:gap-14">
                    <div class="relative flex min-h-105 items-end overflow-hidden rounded-card lg:min-h-140">
                        {{--
                            Изображение декоративное: смысл несёт текст поверх
                            него, поэтому пустой `alt` и `aria-hidden`.
                            В отличие от хиро — `loading="lazy"`: блок стоит
                            ниже первого экрана, и правило LCP из `RULES.md`
                            к нему не относится.

                            Фотография стоковая, из макета, и подлежит замене
                            заказчиком — строка про это стоит во «внешних
                            зависимостях» роадмапа рядом с `hero-*.webp`
                            и `lead-bg.webp`. Снаружи страница выглядит
                            готовой, и заметить чужой кадр больше некому.
                        --}}
                        <img
                            src="{{ Vite::asset('resources/images/service-panel.webp') }}"
                            width="720"
                            height="961"
                            loading="lazy"
                            alt=""
                            aria-hidden="true"
                            class="absolute inset-0 size-full object-cover"
                        >

                        {{-- Маска: подпись лежит на самом светлом участке
                             кадра, поэтому нижний стоп непрозрачный —
                             приглушённый текст поверх блика нечитаем.

                             Стопы макета (`96 → 62 → 10`) заменены вехой 4.11
                             вместе со светлой темой: подпись попадала
                             примерно на 78% непрозрачности, и тёмному тексту
                             поверх мутного серого этого не хватало (около
                             3:1 при минимуме 4.5:1). Непрозрачная часть
                             поднята к 30% высоты — ровно под подпись.
                             Разбор целиком — у такой же панели на главной
                             (`home/index.blade.php`, блок «Экосистема»);
                             панели обязаны остаться одинаковыми, кадр
                             у них один. --}}
                        <div class="absolute inset-0 bg-gradient-to-t from-page via-page/88 via-30% to-page/8"></div>

                        {{-- Подпись живёт в шаблоне, а не в настройках: она
                             описывает конкретный кадр и меняется только вместе
                             с ним. Настройка под неё означала бы, что заказчик
                             может подписать фотографию, которую не может
                             заменить. --}}
                        <div class="relative p-7">
                            <div class="mb-4 text-[13px] tracking-[0.2em] text-accent uppercase">Мастерская</div>

                            <h3 class="mb-3 font-display text-[22px] leading-[1.25] font-semibold">
                                Тот же сервис, что готовит наши автомобили к выдаче
                            </h3>

                            <p class="text-sm leading-[1.65] text-ink-muted">
                                Обслуживаться в ЛАО КАРС можно независимо от того, где куплен автомобиль.
                            </p>
                        </div>
                    </div>

                    <div>
                        <div class="mb-4 text-[13px] tracking-[0.2em] text-accent uppercase">Почему сюда</div>

                        <h2 class="font-display text-3xl font-semibold lg:text-[34px]">
                            Сервис, который <span class="text-accent">не заканчивается на выдаче</span>
                        </h2>

                        {{-- Разметка карточки повторяет блок «Почему мы»
                             главной: формат значения у настроек один и тот же,
                             и две разные карточки на одинаковых данных
                             разъехались бы на первой же правке одной из них. --}}
                        <div class="mt-9 grid gap-5 sm:grid-cols-2">
                            @foreach ($advantages as $advantage)
                                <div class="rounded-card border border-line bg-surface p-7 transition duration-250 hover:-translate-y-1 hover:border-accent/30">
                                    @if ($advantage['number'] !== null)
                                        <div class="mb-5.5 flex size-10 items-center justify-center rounded-full bg-accent-solid/14 font-display text-[15px] font-bold text-accent">
                                            {{ $advantage['number'] }}
                                        </div>
                                    @endif

                                    <h3 class="mb-3 text-[17px] font-semibold">{{ $advantage['title'] }}</h3>

                                    @if ($advantage['text'] !== null)
                                        <p class="text-sm leading-relaxed text-ink-muted">{{ $advantage['text'] }}</p>
                                    @endif
                                </div>
                            @endforeach
                        </div>
                    </div>
                </div>
            </section>
        @endif

        {{-- Форма с выбором конкретной услуги: `:services` включает селект
             «Интересует». Раскладка центрированной карточкой — та же, что
             на остальных страницах разделов (`heading` не задан). --}}
        <x-lead-section :services="$services" title="Записаться на сервис" submit="Записаться" />
    </div>
@endsection
