<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Блоки главной страницы
    |--------------------------------------------------------------------------
    |
    | Значения читаются через env() именно здесь, внутри config-файла:
    | вызов env() из контроллера или сервиса после `php artisan config:cache`
    | вернёт null (антипаттерн зафиксирован в ARCHITECTURE.md).
    |
    | Потолок подборки автомобилей живёт не здесь, а в `catalog.homepage_limit`:
    | он про каталог и попал туда вехой 4.2 вместе с остальными его лимитами.
    |
    */

    // Сколько отзывов показывает главная. Три — ровно столько мест
    // в трёхколоночной сетке макета.
    //
    // Рост числа не ломает вёрстку (сетка переносит), но ломает замысел:
    // отзывы стоят между прайсом и FAQ, и лента из десяти карточек
    // отодвигает FAQ и форму заявки за пределы внимания. Полный список
    // отзывов — задача страницы «О компании», а не главной.
    'reviews_limit' => (int) env('HOME_REVIEWS_LIMIT', 3),

    // Сколько позиций прайса показывает витрина услуг на главной — в каждой
    // категории отдельно. Три — столько же, сколько мест в карточке макета.
    //
    // Витрина усечена намеренно: полный прайс из восемнадцати строк вытеснил
    // бы с главной всё, ради чего человек на неё пришёл. Селект «Интересует»
    // в форме заявки при этом усечению НЕ подлежит и остаётся надмножеством
    // витрины (решение 3 вехи 4.6): клик по видимой строке обязан находить
    // свою опцию, а посетитель, пришедший за позицией вне витрины, обязан
    // найти её в списке.
    'services_per_category' => (int) env('HOME_SERVICES_PER_CATEGORY', 3),

];
