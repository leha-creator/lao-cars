<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Приём заявок с публичных форм
    |--------------------------------------------------------------------------
    |
    | Значения читаются через env() именно здесь, внутри config-файла:
    | вызов env() из провайдера или сервиса после `php artisan config:cache`
    | вернёт null (антипаттерн зафиксирован в ARCHITECTURE.md).
    |
    */

    'rate_limit' => [
        // Заявок в минуту с одного IP. Пять — с запасом для человека
        // и тесно для скрипта. Лимит общий за NAT: за одним адресом
        // сидит офис или мобильный оператор, поэтому срабатывание
        // пишется в канал `leads` на DEBUG — жалоба «форма не
        // отправляется» приходит без следа в обычных логах.
        'per_minute' => (int) env('LEADS_RATE_LIMIT_PER_MINUTE', 5),
    ],

];
