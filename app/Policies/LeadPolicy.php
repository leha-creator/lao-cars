<?php

declare(strict_types=1);

namespace App\Policies;

/**
 * Заявки — вторая половина работы менеджера.
 *
 * Политика написана до появления ресурса заявок (веха 3.7) намеренно:
 * роль менеджера без прав на заявки — половина определения, и проверяется
 * она напрямую через `Gate`, а не через несуществующий экран.
 *
 * @see StaffPolicy — матрица прав живёт в базовом классе
 */
final class LeadPolicy extends StaffPolicy {}
