<?php

declare(strict_types=1);

namespace App\Http\Controllers;

use Illuminate\Contracts\View\View;

/**
 * Главная страница.
 *
 * До вехи 4.1 главная жила замыканием в `routes/web.php` — последним
 * публичным роутом без контроллера. Переезд сделан здесь, а не вехой 4.2:
 * веха всё равно заводит три соседних контроллера, а веха о содержимом
 * главной не должна начинаться с переноса файлов вместо вёрстки.
 *
 * Содержимое собирает веха 4.2: хиро, бегущая строка, подборка авто
 * с отметкой «на главной», блок преимуществ, промо-баннер. Здесь пока
 * заглушка на каркасе — данных контроллер ещё не готовит.
 */
final class HomeController extends Controller
{
    public function index(): View
    {
        return view('home.index');
    }
}
