-- Отдельная база под тесты.
--
-- Smoke-тесты каркаса ходят в реальные PostgreSQL и Redis, а не в SQLite и
-- array-драйверы: подмена драйверов скрыла бы ровно те ошибки сборки
-- инфраструктуры, которые эти тесты и должны ловить. Изоляция от рабочих
-- данных — через отдельную базу здесь и `REDIS_DB=1` в `phpunit.xml`.
--
-- Скрипт выполняется только при первичной инициализации пустого volume
-- (см. комментарий в compose.yml). Владелец — пользователь из POSTGRES_USER,
-- под которым запускается entrypoint.

CREATE DATABASE laocars_testing;
