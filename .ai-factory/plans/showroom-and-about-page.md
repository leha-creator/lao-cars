# План: «Шоу-рум в Москве» и страница «О компании» (веха 4.5, первая половина)

**Ветка:** не создаётся (`git.create_branches: false` в `.ai-factory/config.yaml`) — работа в `master`
**Дата создания:** 2026-08-12
**Файл плана:** `.ai-factory/plans/showroom-and-about-page.md`
**Источник требований:** `.ai-factory/plans/meeting-2026-08-11-launch-fixes.md`, пункт B2

## Настройки

| Параметр | Значение |
| :---- | :---- |
| Тесты | Да. `LayoutTest` правится обязательно — он стережёт состав шапки **двумя списками сразу** (имена роутов со строгим `toBe` и подписи), и пятый пункт уронит оба. Новый `AboutPageTest` по образцу `SectionPagesTest`. В сам `SectionPagesTest` добавляется `/about` в три перечня адресов — контракта формы заявки это не касается, поэтому требование «без правок» не нарушается. `HomePageTest`, `LeadStoreTest`, `LeadFormMarkupTest`, `CatalogIndexTest` обязаны пройти **без правок**; сторож реестра настроек (`ManageSiteSettingsTest`) сверяет ключи с сидом в обе стороны и обязан покраснеть, если правку внесли в одно место из двух |
| Логирование | По смыслу. Новая страница читает настройки и две модели; WARN там, где блок исчезает из-за незаполненных данных, — по образцу главной (`HomeContent`), а не на каждый рендер |
| Документация | Да — обязательный чекпоинт `/aif-docs` по завершении. Плюс статья справки `team-page.md` (веха 4.8) уже обещает сотруднику страницу «О компании» **и заглушку вместо отсутствующего фото** — с этим планом первое обещание становится правдой, второе обязано стать правдой вместе с ним |
| Объём | Переименование «Физический салон» → «Шоу-рум в Москве», фотография салона в блоке доверия, страница «О компании» (история, миссия, команда, отзывы), пункт меню в шапке после «Запчасти» |
| Вне плана | **Вёрстка страницы контактов** — вторая половина вехи 4.5; на встрече не обсуждалась |
| Смежные планы — **выполнены** | Статус «В пути» (веха 4.10, 13.08), палитра и переименования меню (веха 4.11, 13.08), иллюстрации этапов (веха 4.9, 12.08). Все три закрыты до этого плана, поэтому их условные оговорки ниже заменены фактами: подписи меню уже «Автомобили» и «Сервис», светлая тема уже действует, `docs/design-system.md` уже требует верстать «О компании» в новой палитре |
| Внешняя зависимость | **Настоящая фотография шоу-рума от заказчика.** Без неё задача 2 не выполняется — см. решение 3 |

## Привязка к роадмапу

**Веха:** «4.5 О компании и контакты» — существующая, в `ROADMAP.md` не отмечена закрытой.

**Обоснование:** страница «О компании» — ровно её содержимое, и роут `about.index` там назван заранее: «его роут должен называться `about.index`: пункт уже объявлен в `App\Support\SiteMenu` для подвала и появится там автоматически вместе с роутом». Новую веху заводить не нужно.

**Веха закрывается наполовину, и это записано честно.** Её вторая половина — вёрстка страницы контактов (сейчас там заглушка вехи 4.1, и оговорка «до вехи 4.5 в публичный релиз страница не идёт» стоит в шаблоне `resources/views/contacts/index.blade.php:11` — в PHPDoc `ContactController` её нет, там сказано мягче: «полная вёрстка приходит вехой 4.5»). Контакты на встрече не обсуждались и в объём этого плана по решению не вошли. Следствие: **пункт роадмапа остаётся неотмеченным** после выполнения этого плана, а в его формулировку добавляется пометка о состоянии. Отметить веху закрытой при незавёрстанных контактах значило бы записать в роадмап неправду, а нужный кусок работы потерять.

**Правку `ROADMAP.md` делает задача 7**, напрямую и строго по перечню: скилл `/aif-roadmap` в проекте не установлен (прецеденты — вехи 4.7 и 4.8).

## Исходное состояние

Проверено по коду **после вех 4.9–4.11** (12–13.08.2026). Номера строк — фактические.

- **Роута `about.index` нет.** В `routes/web.php` объявлены `home`, `catalog.index`, `catalog.show`, `services.index`, `parts.index`, `contacts.index`, `leads.store` и две ручки push-подписок. Контроллера `AboutController` тоже нет.
- **`SiteMenu` к этому готов.** `LABELS` уже содержит `'about.index' => 'О компании'` (строка 51), `FOOTER` уже включает `about.index` между `parts.index` и `contacts.index` (строки 73-79). Метод `links()` выбрасывает пункты без зарегистрированного роута. **В подвале пункт появится сам, без единой правки.**
- **`SiteMenu::HEADER` — четыре пункта** (строки 59-64), и устаревать при правке будут **три докблока сразу**, а не один: `HEADER:55` («закреплённое меню шапки — четыре пункта из ТЗ»), `FOOTER:66-68` («трафик она не приносит, а пункт меню стоит дорого» — то самое объяснение, которое заказчик отменил на встрече, 07:04–07:15) и `links():108-110` («„О компании“ появляется в подвале вехой 4.5 вместе со своим роутом» — переходный период кончается).
- **`LayoutTest` содержит ДВА списка меню, а не один.** Строка 92 — `$expected = ['catalog.index', 'services.index', 'parts.index', 'contacts.index']` со строгим `expect(array_column(SiteMenu::header(), 'name'))->toBe($expected)`. Строка 107 — перечень подписей `['Автомобили', 'Сервис', 'Запчасти', 'Контакты']`. Подписи уже переименованы (веха 4.11 и ответ заказчика 13.08), то есть «Каталог» и «Автосервис» в тесте искать бесполезно. Пятый пункт уронит оба списка, и это правильное поведение сторожа.
- **`home/index.blade.php:264`** (не 244 — веха 4.11 вставила выше комментарий про светлые секции) — первая карточка полосы доверия: `['title' => 'Физический салон в Москве', 'text' => 'Можно увидеть автомобили и познакомиться с командой.']`. Блок — четыре текстовые карточки подряд в сетке `sm:grid-cols-2 lg:grid-cols-4` внутри `<section class="theme-light …">`, тексты живут в шаблоне намеренно (веха 4.6: одна строка на карточку, привязанная к вёрстке).
- **Формулировка «физический салон» встречается в коде РОВНО ОДИН раз** — в этой строке. Проверено поиском по проекту: ни в `seo.default_description`, ни в `home.advantages`, ни на страницах разделов её нет. Остальные вхождения — расшифровка встречи, макеты (`assets/`) и сами планы.
- **Полоса доверия задокументирована, и документация станет неверной.** `docs/homepage.md:15` — строка таблицы «Полоса доверия | светлый | **статика шаблона** | **не бывает пустой**»; `docs/homepage.md:33` — правило «тексты живут в шаблоне… понадобится их править — заводится настройка, но не раньше первой такой просьбы». Задача 2 заводит там настройку и условный блок, то есть срабатывает названный триггер.
- **Модели под страницу готовы.** `Employee` — скоупы `published()`, `ordered()`, атрибут `photoUrl`, связь `media()`; его PHPDoc (строки 57-61) прямо требует грузить списки с `with('media')`, иначе N+1. `Review` — `published()`, `pending()`, `ordered()`, `photoUrl`, `media()`. Оба засеяны (`EmployeeSeeder`, `ReviewSeeder`), фабрики есть. Отзывы уже выводятся на главной (веха 4.6) — данные и модерация работают.
- **Справка сотруднику уже обещает поведение, которого в коде нет.** `resources/help/team-page.md:22`: «Карточка без фотографии выглядит нормально — **на её месте показывается заглушка**». Заглушки не существует нигде: карточка отзыва на главной при пустом `photo_url` просто не рендерит `<img>` (`home/index.blade.php:1247`). Обещание становится проверяемым в момент открытия страницы.
- **Тексты страниц разделов приходят из настроек, а не из шаблона.** Образец — `services_page.intro_title`, `services_page.intro_text`, `parts_page.intro_title`, `parts_page.intro_text`. Роадмап это фиксирует правилом: «заменить настройку константой из макета значит отобрать у заказчика поле, которое ему уже отдали».
- **Вкладки «Страницы» в `ManageSiteSettings` НЕТ.** Метод `pagesTab()` (строка 508) создаёт `Tab::make('Автосервис и запчасти')`, и вкладок в форме шесть: Контакты, Соцсети, Главная, Подвал, Автосервис и запчасти, SEO. Имя вкладки намеренно не трогали при переименовании меню — это записано и в `DESCRIPTION.md:13`, и в `docs/design-system.md:306`, — поэтому «дописать `about_page.*` во вкладку «Страницы»» невыполнимо буквально и вредно по смыслу.
- **Реестр настроек сверяется с сидом в обе стороны.** `ManageSiteSettingsTest:161` — тест `has a key registry matching the seeder`: `array_diff` в обе стороны между ключами `SiteSettingSeeder` и `ManageSiteSettings::settingKeys()`. Новый ключ, добавленный в одно место из двух, красит тест.
- **`config/home.php:26-27` резервирует полный список отзывов за страницей «О компании».** Дословно: «Полный список отзывов — задача страницы «О компании», а не главной». Лимит `home.reviews_limit = 3` заведён с этой оговоркой, и взять его на новую страницу значит противоречить собственному обоснованию конфига (см. решение 8).
- **Кнопка «Оставить заявку» в шапке ведёт на `#lead-form` на КАЖДОЙ странице** — `site-header.blade.php:78` (десктоп) и `:150` (мобильное меню). Якорь существует только там, где отрендерен `x-lead-section` (`lead-section.blade.php:36`). Страница без секции заявки даёт мёртвую кнопку в шапке, молча и без ошибок.
- **Десктопная шапка на `lg` тесная.** `<nav class="hidden min-w-0 shrink items-center gap-6 text-sm whitespace-nowrap lg:flex xl:gap-7">` (`site-header.blade.php:37`) делит одну строку с логотипом, телефоном текстом и кнопкой заявки; `whitespace-nowrap` не даёт переносу спасти раскладку. Мобильное меню при этом — вертикальный список в оверлее `fixed inset-0` (`:106`, `:131`), и пятый пункт ему безразличен.
- **Шапка внутренней страницы — готовый компонент** `x-page-heading` с `eyebrow`, `title` (строкой или слотом), `intro` и именованным слотом `below`. Вехой 4.11 он научился принимать атрибуты с места вызова (`:53`) — именно так на него вешается `theme-light`.
- **Светлая тема уже действует, и требование к этой странице уже записано.** Веха 4.11 закрыта 13.08 (`ROADMAP.md:69`). `docs/design-system.md:191`: «„Шоу-рум“ и „О компании“ (веха 4.5) верстаются сразу в новой палитре». Рецепт «Как добавить страницу» (`docs/design-system.md:312-333`) задаёт образец шаблона — `x-page-heading class="theme-light"`, светлые секции, `x-lead-section` внизу. `PaletteGuardTest` обходит все публичные blade-файлы рекурсивно, то есть новый шаблон попадает под сторожа автоматически.
- **`docs/design-system.md` устареет в трёх местах:** `:171` — таблица «Где что светлое» без строки про «О компании»; `:301` — карта меню с комментарием `// только подвал, роут приходит вехой 4.5`; `:308` — «`LayoutTest` проверяет, что все четыре роута зарегистрированы».
- **`DESCRIPTION.md:13` утверждает «четыре закреплённых пункта в шапке на всех страницах».** После задачи 5 это неправда, и владельца у файла в исходном плане не было — отсюда задача 8.
- **`SectionPagesTest` перечисляет адреса руками:** датасеты на строках 28 и 91 (`['/', '/services', '/parts', '/contacts']`) и перечень на строке 70. Новая страница в них не попадает сама, то есть останется единственной публичной без трёх сквозных сторожей.
- **`HomePageTest` полосу доверия не проверяет вовсе** — поиск по `tests/` не даёт ни одного вхождения её текстов. Значит правка задачи 1 сторожа не имеет, и его стоит завести.
- **Макет «О компании» есть.** Роадмап: макет v1 (`assets/Макет сайта «ЛАО КАРС»/`) содержит готовый экран About. Это единственная из оставшихся страниц, для которой макет существует, — контакты собираются по UI Kit. Светлых значений в макете нет вовсе (он рисовался под тёмную тему), поэтому раскладка берётся из него, а цвета — из токенов.
- **Фотографии в вёрстке — стоковые заглушки с непроверенными правами** (`hero-*.webp`, `lead-bg.webp`, `service-panel.webp`). Записано в роадмапе как внешняя зависимость, блокирующая запуск. **Там же, `ROADMAP.md:129`, уже стоит запись про фотографию шоу-рума** — её внесла веха 4.9 вместе с разрешением на нейросетевые иллюстрации этапов. Заводить второй такой пункт не нужно.

## Ключевые решения

**1. «Шоу-рум в Москве» — правка формулировки, которую заказчик сделал сам.** «Не физический салон, а шоу-рум в Москве. — У нас и шоу-рум, и сервис». Правка одной строки в шаблоне; текст живёт там намеренно (веха 4.6), и переносить его в настройки ради одного слова не нужно — триггер завести настройку назван в самом комментарии блока и это не он.

**2. Пункт «О компании» встаёт в шапку после «Запчасти» — решение принято на встрече, а не выведено.** Заказчик рассматривал два места вслух и выбрал: «либо после запчастей, либо после контактов — после запчастей, наверное» (07:04–07:15). Контакты остаются отдельным пунктом и уезжают на пятую позицию.

**3. Решение вехи 4.1 «в закреплённое меню не входит» пересматривается по решению заказчика, и это записывается в PHPDoc.** Иначе следующий, кто прочтёт константу `FOOTER` с её объяснением «пункт меню стоит дорого», уберёт пункт обратно — ровно так же, как в проекте уже описан риск с «поправят по макету». Формулировка должна называть автора решения: не «передумали», а «заказчик потребовал на демонстрации 11.08».

**4. Фотография шоу-рума обязана быть настоящей — стоковая здесь не заглушка, а обман.** Заказчик сформулировал задачу блока прямо: «чтобы человек открыл и понял: они в реале есть, можно прийти посмотреть». Стоковый или нейросетевой интерьер автосалона отвечает на этот вопрос ложью, и цена ошибки не «некрасиво», а «клиент приехал и не узнал место». Это отличает задачу 2 от иллюстраций этапов покупки, где временные нейросетевые изображения допустимы прямым разрешением заказчика (22:55–23:10): там картинка иллюстрирует процесс, здесь — свидетельствует о факте.

**Следствие:** задача 2 блокируется внешней зависимостью и **не заменяется заглушкой**. Нет фотографии — блок остаётся текстовым, как сейчас, и это записывается, а не обходится.

**5. Тексты страницы «О компании» приходят из настроек, а не из шаблона, и живут в СВОЕЙ вкладке.** Правило проекта: H1 и вводные тексты страниц разделов редактируются заказчиком (`services_page.*`, `parts_page.*`), и сторож — `SectionPagesTest`. Новая страница получает свою группу `about_page.*` по тому же образцу. Константа из макета отобрала бы у заказчика поле, которое ему уже отдали на двух других страницах, — и он заметит асимметрию первым.

**Вкладка — новая, седьмая, «О компании».** Существующая вкладка называется «Автосервис и запчасти», а не «Страницы»: её имя намеренно не трогали при переименовании меню, и это записано в двух документах (`DESCRIPTION.md:13`, `docs/design-system.md:306`). Дописать ключи страницы «О компании» под заголовок про автосервис значит спрятать их там, где заказчик их не найдёт. Переименование вкладки в «Страницы» — допустимая альтернатива, но тогда правка идёт вместе с обеими строками документации **в том же коммите**: иначе следующая сверка вернёт прежнее имя со ссылкой на документ.

**6. Команда и отзывы берутся из существующих моделей, новых сущностей не заводится.** `Employee` и `Review` заведены вехой 3.2, панель к ним — вехой 3.5, модерация отзывов работает и уже имеет потребителя на главной (веха 4.6). Страница «О компании» — второй потребитель `Employee`, и до неё у модели не было ни одного: это и есть та веха, ради которой её заводили.

**7. Данные страницы собирает сервис, а не контроллер.** Граница проведена в `ARCHITECTURE.md` и разведена там же по обе стороны на паре «автосервис / запчасти»: `ServicesPageContent` есть, `PartsPageContent` нет намеренно. Здесь — три источника (две модели плюс группа настроек), нормализация репитера истории и разрешение `media_id` в URL, то есть ровно та работа, из-за которой существует `HomeContent`. Сервис называется `AboutPageContent`, и в PHPDoc контроллера записывается, почему он есть, — по тому же правилу, по которому в `PartsController` записано, почему его нет.

**8. Отзывы на «О компании» — те же данные и тот же способ выбирать, но БЕЗ лимита главной.** Первоначальная формулировка решения («тот же лимит, что на главной») противоречила коду и была исправлена при доработке плана: `config/home.php:26-27` объясняет выбор тройки дословно так — «Рост числа не ломает вёрстку, но ломает замысел: отзывы стоят между прайсом и FAQ… **Полный список отзывов — задача страницы «О компании», а не главной**». То есть лимит 3 заведён с прямой оговоркой, что на этой странице его быть не должно.

Общими остаются модель, скоуп `published()->ordered()` и загрузка `with('media')` — второй способ выбирать отзывы разошёлся бы с первым, и посетитель увидел бы на двух страницах разный порядок. Разным остаётся объём: главная показывает витрину из трёх, «О компании» — все опубликованные. Если по ходу выяснится, что потолок всё же нужен (сотня отзывов на одной странице), он заводится **своим** ключом (`about.reviews_limit`), а `home.reviews_limit` не переиспользуется, — и комментарий `config/home.php` правится тем же коммитом. Оставить два противоречащих утверждения в конфиге и в плане нельзя: победит то, которое прочтут вторым.

**9. Блок без данных не рендерится вовсе** — правило проекта, действующее на всей главной. Нет опубликованных сотрудников — нет секции «Команда», а не заголовок над пустой сеткой. Фолбэк «показать что-нибудь» отклонён: он прячет незаполненные данные. Вместо фолбэка — WARN в лог, по тому же основанию, по которому его пишет шапка с незаполненным телефоном.

**10. Контакты остаются вне плана, и веха остаётся открытой.** Соблазн «дозакрыть веху заодно» отклонён: контакты на встрече не обсуждались, их вёрстка собирается по UI Kit без макета, и это отдельный кусок работы, а не довесок. Честная запись в роадмапе стоит дешевле, чем половина страницы, сделанная по инерции.

## План коммитов

- **Коммит 1** (задачи 1–2): `feat: шоу-рум в Москве в блоке доверия на главной`
- **Коммит 2** (задачи 3–4): `feat: страница «О компании» — роут, данные и вёрстка`
- **Коммит 3** (задача 5): `feat: «О компании» в закреплённом меню шапки`
- **Коммит 4** (задачи 6–9): `test: страница «О компании»` + `docs: раздел «О компании»` + описание проекта и роадмап

**Задача 2 заблокирована внешней зависимостью и коммит 1 не блокирует.** Если фотографии от заказчика нет, коммит 1 уходит с одной задачей 1 (переименование), а задача 2 переносится отдельным коммитом позже. Задачи 3–9 её не ждут — см. «Зависимости» у задачи 3.

## Задачи

### Фаза 1 — Правки на главной

- [x] **Задача 1: «Физический салон в Москве» → «Шоу-рум в Москве»**

  `resources/views/home/index.blade.php:264` — заменить заголовок первой карточки полосы доверия. Текст под ним («Можно увидеть автомобили и познакомиться с командой») заказчик не правил — оставить.

  **Искать формулировку по проекту не нужно — это уже сделано при доработке плана.** «Физический салон» встречается в коде ровно один раз, в этой строке: ни в `seo.default_description`, ни в `home.advantages`, ни на страницах разделов её нет. Остальные вхождения — расшифровка встречи, HTML-макеты в `assets/` и сами планы; макеты не правятся принципиально, они снимок исходника.

  Номер строки в первой редакции плана (`:244`) устарел: веха 4.11 вставила выше комментарий про светлые секции. Ориентироваться на текст, а не на номер.

  **Сторожа у этой строки нет** — `HomePageTest` полосу доверия не проверяет вовсе. Сторож заводится задачей 6, здесь только правка.

  **Логирование:** нет.

  **Файлы:** `resources/views/home/index.blade.php`

- [x] **Задача 2: фотография шоу-рума в блоке доверия**

  > **Выполнено с отступлением от решения 4, по прямому указанию заказчика 13.08.2026.** Настоящей фотографии салона на Осенней, 17 нет; в блок поставлен присланный кадр (`assets/showroom.png`) — **чужой дилерский центр** (шоу-рум Zeekr, Сингапур, 9 Leng Kee Road; на фасаде читается логотип бренда и адрес). Риск был назван до выполнения и подтверждён решением: блок отвечает на вопрос «место существует, можно приехать» кадром чужого здания. Механизм замены заведён целиком — ключ `home.trust.image_id`, пикер в панели, — то есть замена делается заказчиком без разработчика. Статус записан в роадмапе задачей 9.

  Блок полосы доверия сейчас — четыре текстовые карточки подряд, и это ровно та «стена текста», на которую заказчик жаловался весь звонок. Задача: ввести в блок фотографию салона.

  **Задача заблокирована внешней зависимостью и заглушкой не подменяется** (решение 4). Порядок:

  1. запросить у заказчика фотографию шоу-рума. Требование к кадру сформулировать его же словами: по фотографии должно быть понятно, что место существует и туда можно приехать;
  2. фотография загружается в медиабиблиотеку через панель — `ImageProcessor` сам сделает WebP, ресайз и превью, готовить веб-формат не нужно;
  3. вёрстка: фотопанель рядом с карточками, по образцу блока «почему сюда» на странице автосервиса (`service-panel.webp`) и блока экосистемы на главной. Раскладку держать в правиле «не более двух фоновых оттенков на экране»;
  4. если фотографии нет — **блок остаётся текстовым**, задача переносится, и это записывается в плане явно. Не подставлять стоковое изображение автосалона.

  Ссылка на изображение хранится ключом настроек (образец — `home.promo.image_id`), а не именем файла в шаблоне: заказчик обязан иметь возможность заменить фотографию сам. Новый ключ добавляется **и** в `ManageSiteSettings`, **и** в `SiteSettingSeeder`, **и** в `MediaSettingKeys` — последнее обязательно, иначе используемую фотографию можно будет удалить из библиотеки, и ссылка станет висячей. Сид обязан задать форму значения (`'image_id' => null`) по той же причине, по которой её задают `home.promo` и `home.steps`: форма пишет ключ безусловно, и на свежезасеянной базе шаблон обратился бы к отсутствующему индексу.

  **Документация правится этой же задачей.** `docs/homepage.md:15` — строка таблицы утверждает про полосу доверия «статика шаблона» и «не бывает пустой», после правки неверно и то и другое. `docs/homepage.md:33` называет триггер: «тексты живут в шаблоне… понадобится их править — заводится настройка, но не раньше первой такой просьбы». Просьба пришла — значит правило сработало штатно, и это стоит записать, а не молча обойти.

  **Логирование:** WARN, если ключ настройки указывает на удалённую запись библиотеки — по образцу `HomeContent::promoImageUrl()`. Это единственный отказ блока, который снаружи выглядит как «просто нет картинки».

  **Файлы:** `resources/views/home/index.blade.php`, `app/Services/HomeContent.php`, `app/Filament/Pages/ManageSiteSettings.php`, `app/Support/MediaSettingKeys.php`, `database/seeders/SiteSettingSeeder.php`, `docs/homepage.md`
  **Зависит от:** задачи 1
  **Не блокирует:** задачи 3–9 (см. «Зависимости» задачи 3)

<!-- Чекпоинт коммита: задачи 1–2 -->

### Фаза 2 — Страница «О компании»

- [x] **Задача 3: роут, контроллер, сервис и настройки**

  - `routes/web.php` — `Route::get('/about', [AboutController::class, 'index'])->name('about.index');`. **Имя роута обязано быть `about.index`** — оно уже объявлено в `SiteMenu::LABELS` и `FOOTER`, и любое другое означает, что пункт подвала так и не появится (метод `links()` молча выбросит его).
  - `app/Http/Controllers/AboutController.php` — тонкий: принял, делегировал, отдал. В PHPDoc записать, почему у страницы есть сервис, — по образцу `ServiceController`/`PartsController`, где записана причина в обе стороны.
  - `app/Services/AboutPageContent.php` — собирает историю и миссию из настроек, опубликованных сотрудников (`Employee::published()->ordered()->with('media')`), опубликованные отзывы (`Review::published()->ordered()->with('media')`, все — решение 8) и SEO-заголовки. **`with('media')` обязателен у обоих**: PHPDoc `Employee::photoUrl()` (строки 57-61) требует этого дословно, а без него сетка команды даёт N+1 — запрос на каждую карточку. Образец — `HomeContent::reviews()`, где связь уже подгружается. Нормализует репитер истории так же, как `HomeContent` нормализует `steps` и `advantages`: форма настроек пишет ключи безусловно, а репитер при удалении всех элементов отдаёт `null`, а не `[]` — шаблон, написанный по сиду, упадёт на первом же `foreach (null)` и упадёт на проде.
  - Настройки — новая группа `about_page.*` в **новой, седьмой вкладке «О компании»** `ManageSiteSettings` (решение 5): вводный заголовок и текст (по образцу `services_page.intro_title` / `intro_text`), миссия, история (репитер). Вкладки «Страницы» в форме нет — существующая называется «Автосервис и запчасти» (`pagesTab()`, строка 508), и её имя намеренно не трогали при переименовании меню. Если вместо новой вкладки выбирается переименование существующей — правятся ещё и `DESCRIPTION.md:13`, и `docs/design-system.md:306`, тем же коммитом.
  - **Те же ключи добавить в `SiteSettingSeeder`.** Сторож `has a key registry matching the seeder` (`ManageSiteSettingsTest:161`) сверяет два списка в обе стороны, и правка одного из них покрасит тест — это и есть его работа. У репитера истории сид задаёт форму значения так же, как `home.steps`.

  **Логирование:** WARN, когда блок исчезает из-за незаполненных данных (нет опубликованных сотрудников — нет секции «Команда»), по образцу `HomeContent`. Не логировать сам рендер: это чтение.

  **Файлы:** `routes/web.php`, `app/Http/Controllers/AboutController.php`, `app/Services/AboutPageContent.php`, `app/Filament/Pages/ManageSiteSettings.php`, `database/seeders/SiteSettingSeeder.php`
  **Зависит от:** задачи 1

  **Почему НЕ от задачи 2.** Задача 2 заблокирована внешней зависимостью — настоящей фотографией от заказчика, — и по решению 4 заглушкой не подменяется. Данных задача 3 у неё не берёт: общий у них ровно один файл (`SiteSettingSeeder`), и это не зависимость по данным, а соседство в диффе. Линейная цепочка «3 после 2» означала бы, что отсутствие одной фотографии останавливает всю страницу «О компании» вместе с меню, тестами и документацией.

- [x] **Задача 4: вёрстка страницы**

  `resources/views/about/index.blade.php` — по экрану About макета v1 (это единственная из оставшихся страниц, для которой макет есть). Из макета берётся раскладка, а не цвета: светлых значений в нём нет вовсе. Собирается из готовых компонентов по рецепту `docs/design-system.md:312-333`: `x-page-heading` для шапки, карточки на токенах дизайн-системы, `x-lead-section` внизу.

  Блоки: история и миссия, команда карточками сотрудников, отзывы. Подробности из блока доверия на главной, которые заказчик просил перенести сюда (05:47–07:15), — сюда же.

  **Секция заявки на странице обязательна — это не вопрос вкуса.** Кнопка «Оставить заявку» в шапке ведёт на `#lead-form` на каждой странице сайта (`site-header.blade.php:78` — десктоп, `:150` — мобильное меню), а якорь появляется только вместе с `x-lead-section` (`lead-section.blade.php:36`). Страница без секции даёт мёртвую кнопку в шапке — без ошибок, без записи в лог, заметно только кликом. По той же причине `x-lead-section` стоит в шаблоне-образце проекта. Открытый вопрос заказчику остаётся только про формулировку заголовка секции, а не про её наличие.

  **Карточка сотрудника без фотографии обязана показывать заглушку.** Это уже обещано сотруднику в справке: `resources/help/team-page.md:22` — «Карточка без фотографии выглядит нормально — на её месте показывается заглушка». Заглушки в проекте нет: карточка отзыва на главной при пустом `photo_url` просто не рендерит `<img>` (`home/index.blade.php:1247`), и скопировать этот приём сюда значит превратить статью справки в ложь в момент открытия страницы. Заглушка — на токене `bg-photo` (он уже используется под фото отзыва) с той же геометрией, что и снимок.

  Правила, действующие на всех страницах проекта и здесь тоже:

  - **блок без данных не рендерится вовсе** (решение 9) — ни заголовка, ни пустой сетки. Заглушка фото — исключение ровно на один уровень ниже: пустая **карточка** не рендерится, карточка без **фото** рендерится с заглушкой;
  - Blade в базу не ходит: всё готовит `AboutPageContent`;
  - meta-теги — из `seo.default_*` с фолбэком в шаблоне, как на остальных страницах;
  - якорь `#lead-form` на странице ровно один.

  **Палитра — не условие, а факт.** Веха 4.11 закрыта 13.08, и `docs/design-system.md:191` уже содержит требование: «„Шоу-рум“ и „О компании“ (веха 4.5) верстаются сразу в новой палитре». Страница светлая целиком, как остальные внутренние: `class="theme-light"` на `x-page-heading` (компонент принимает атрибуты с места вызова) и на каждой секции; секция заявки красится сама по раскладке и `theme-light` не принимает. Приёмка — по разделу «Приёмка светлой секции» того же документа: смотреть на стык секций, а не на середину, и искать тёмные полосы от схлопнувшихся `mb-*`. `PaletteGuardTest` подхватит новый шаблон автоматически — он обходит все публичные blade-файлы рекурсивно, отдельной регистрации не нужно.

  **Логирование:** нет (WARN пишет сервис).

  **Файлы:** `resources/views/about/index.blade.php`
  **Зависит от:** задачи 3

<!-- Чекпоинт коммита: задачи 3–4 -->

### Фаза 3 — Меню

- [x] **Задача 5: «О компании» в шапке после «Запчасти»**

  `app/Support/SiteMenu.php` — добавить `'about.index'` в константу `HEADER` **между `parts.index` и `contacts.index`** (решение 2). `LABELS` и `FOOTER` править не нужно — там пункт уже есть.

  **Устаревают ТРИ докблока `SiteMenu`, а не один.** Каждый написан убедительно и переживёт правку константы, если его не тронуть:

  - `HEADER:55` — «Закреплённое меню шапки — четыре пункта из ТЗ, в этом порядке». Пунктов пять, и пятый из ТЗ не следует;
  - `FOOTER:66-68` — «страница есть в макете и в ТЗ, но в закреплённое меню не входит — трафик она не приносит, а пункт меню стоит дорого». Это то самое объяснение, которое заказчик отменил (решение 3). Записать: решение пересмотрено заказчиком на демонстрации 11.08, пункт в шапке по его прямому указанию, позиция выбрана им же. Формулировать так, чтобы был виден автор решения, а не «передумали»;
  - `links():108-110` — «„О компании“ появляется в подвале вехой 4.5 вместе со своим роутом, и до тех пор пункт просто не выводится». Переходный период кончился; сам механизм отсева остаётся и описан выше по тексту докблока — убирается только абзац про ожидание.

  **В `LayoutTest` правятся ДВА списка, а не один.** Строка 92 — имена роутов, и сравнение там строгое (`toBe`), то есть порядок значим: `['catalog.index', 'services.index', 'parts.index', 'about.index', 'contacts.index']`. Строка 107 — подписи: `['Автомобили', 'Сервис', 'Запчасти', 'О компании', 'Контакты']`. Подписи уже переименованы вехой 4.11 и ответом заказчика 13.08 — «Каталог» и «Автосервис» в файле искать бесполезно. Падение обоих списков до правки — правильное поведение.

  **Проверять надо ДЕСКТОПНУЮ шапку, а не мобильную.** Мобильное меню — вертикальный список в оверлее `fixed inset-0` (`site-header.blade.php:106`, `:131`), пятый пункт ему безразличен. Тесная строка десктопная: `<nav class="hidden min-w-0 shrink items-center gap-6 … whitespace-nowrap lg:flex xl:gap-7">` (`:37`) делит одну строку с логотипом, телефоном текстом и кнопкой «Оставить заявку», а `whitespace-nowrap` не даёт переносу спасти раскладку. Узкое место — диапазон **1024–1280px**: `lg` уже показывает горизонтальное меню, а увеличенный зазор `xl:gap-7` ещё не включился. Смотреть глазами на 1024, 1152 и 1280.

  **Логирование:** нет.

  **Файлы:** `app/Support/SiteMenu.php`, `tests/Feature/Http/LayoutTest.php`
  **Зависит от:** задачи 3

  **Почему от 3, а не от 4.** `SiteMenu::links()` требует зарегистрированного роута, а не свёрстанной страницы, — с точки зрения кода пункт можно поставить сразу после задачи 3. Держать его до конца вёрстки стоит по другой причине, и она человеческая: пункт меню, ведущий на полусобранную страницу, увидит заказчик, а не разработчик. Поэтому коммит 3 идёт после коммита 2 по плану коммитов, но техническая зависимость здесь — задача 3.

<!-- Чекпоинт коммита: задача 5 -->

### Фаза 4 — Проверка, документация, описание проекта и роадмап

- [x] **Задача 6: тесты**

  Новый `tests/Feature/Http/AboutPageTest.php` по образцу `SectionPagesTest`:

  - страница отдаёт 200, H1 берётся из настройки, а не из константы (сторож решения 5 — ровно то правило, которое роадмап защищает `SectionPagesTest`-ом на двух других страницах);
  - опубликованные сотрудники выводятся, неопубликованные — нет;
  - опубликованные отзывы выводятся, ожидающие модерации — нет;
  - **пустые блоки не рендерятся**: нет опубликованных сотрудников — на странице нет ни заголовка «Команда», ни пустой сетки (сторож решения 9);
  - **карточка сотрудника без фотографии рендерится и содержит заглушку** — сторож обещания из `resources/help/team-page.md:22`. Без него статья справки расходится с поведением молча;
  - `null` вместо массива в настройке-репитере не роняет страницу (та самая ловушка, из-за которой `HomeContent` нормализует значения);
  - **число запросов на странице** — сторож против N+1 на сетке команды: без `with('media')` каждая карточка добавляет запрос. Образец — `LayoutTest` «reads site settings once per request», прецедент — сторож этапов покупки вехи 4.9, поймавший 16 запросов против 11;
  - meta-теги на месте.

  Правятся существующие:

  - `tests/Feature/Http/LayoutTest.php` — **два списка** (имена роутов на строке 92 со строгим `toBe` и подписи на строке 107), «О компании» появилась и в подвале (там она возникает сама вместе с роутом — проверить, что это действительно произошло);
  - `tests/Feature/Http/SectionPagesTest.php` — **добавить `/about` в три перечня адресов**: датасеты на строках 28 (`serves every section page`) и 91 (`captures a lead from every section page`) и перечень на строке 70 (`asks for part details only on the parts page`). Адреса там перечислены руками, поэтому новая страница сама в них не попадёт и останется единственной публичной без трёх сквозных сторожей — «отдаёт 200», «заявка с неё доходит до базы», «`part_vin` на неё не протёк». Контракта формы заявки эта правка не касается, то есть требование «`SectionPagesTest` проходит без правок» не нарушается: меняется охват, а не проверяемое поведение;
  - `tests/Feature/Filament/ManageSiteSettingsTest.php` — сторож реестра ключей (строка 161) покраснеет, пока `about_page.*` не добавлены и в форму, и в сид. Правится сид/реестр, а не тест. Репитер истории заодно получает проверку формы значения по образцу `keeps the new home repeaters lists of objects`;
  - `tests/Feature/Http/HomePageTest.php` — **завести** сторож на «Шоу-рум в Москве». Полосу доверия он сейчас не проверяет вовсе (проверено поиском), то есть у правки задачи 1 сторожа нет ни одного, и откат формулировки прошёл бы незамеченным.

  Прогнать весь набор целиком. `LeadStoreTest`, `LeadFormMarkupTest`, `CatalogIndexTest`, `PaletteGuardTest` обязаны пройти **без правок**: контракт формы заявки и палитру этот план не трогает, а новый шаблон `PaletteGuardTest` подхватывает сам.

  **Логирование:** проверить WARN о пустых блоках и WARN о висячей ссылке на медиабиблиотеку (задача 2) — обе записи нужны именно потому, что снаружи оба отказа выглядят как «просто пусто».

  **Файлы:** `tests/Feature/Http/AboutPageTest.php`, `tests/Feature/Http/LayoutTest.php`, `tests/Feature/Http/SectionPagesTest.php`, `tests/Feature/Filament/ManageSiteSettingsTest.php`, `tests/Feature/Http/HomePageTest.php`
  **Зависит от:** задач 1–5

- [x] **Задача 7: документация**

  Через `/aif-docs` (обязательный чекпоинт по настройке плана). Файлы названы поимённо: строки ниже устаревают конкретные, и постфактум их искать некому.

  - **новая страница `docs/` про «О компании»** — такой в проекте нет (есть `homepage.md`, `catalog.md`, `services-and-parts.md`): откуда берутся данные, какие ключи настроек ею управляют, почему у страницы есть сервис (и как это соотносится с парой «автосервис / запчасти», где у одной он есть, а у другой нет намеренно), почему отзывы показываются без лимита главной;
  - **`docs/design-system.md` — три места.** `:171` — таблица «Где что светлое» получает строку про «О компании» (страница светлая целиком, как автосервис, запчасти и контакты). `:301` — карта меню: у `'about.index'` стоит комментарий `// только подвал, роут приходит вехой 4.5`, теперь пункт в шапке и в подвале. `:308` — «`LayoutTest` проверяет, что все **четыре** роута зарегистрированы» и «„О компании“ появится в подвале автоматически, как только веха 4.5 заведёт роут»: обе фразы становятся неверными;
  - **`docs/homepage.md`** — если задача 2 выполнена, строки `:15` и `:33` про полосу доверия правятся ею же; если задача 2 перенесена, здесь ничего не трогать и записать это явно;
  - состав меню: шапка стала пятипунктовой, решение вехи 4.1 пересмотрено заказчиком — та же формулировка, что в PHPDoc `SiteMenu`, чтобы два источника не разъехались;
  - `resources/help/team-page.md` — статья справки (веха 4.8) описывает карточки сотрудников и обещает две вещи: «страница „О компании“ показывает только заполненные карточки» (`:26`) и «карточка без фотографии выглядит нормально — на её месте показывается заглушка» (`:22`). Первое обеспечивает решение 9, второе — заглушка из задачи 4. Сверить оба с фактическим поведением, убрать фразу «сама страница „О компании“ на сайте пока готовится» (`:28`) и дописать про отзывы на странице. Это тот самый случай, ради которого веха 4.8 записала правило: правка, меняющая то, что видит сотрудник, правит статью в том же коммите;
  - `README.md` — абзац текущего состояния;
  - `AGENTS.md` — карта структуры пополняется `AboutController`, `AboutPageContent`, `resources/views/about/`.

  **Логирование:** документация логирования не требует.

  **Файлы:** новая страница в `docs/`, `docs/design-system.md`, `docs/homepage.md`, `resources/help/team-page.md`, `README.md`, `AGENTS.md`
  **Зависит от:** задач 1–6

- [x] **Задача 8: `DESCRIPTION.md` — состав меню и статус страницы**

  Файл описывает проект целиком и после задачи 5 содержит прямую неправду, а владельца у него в первой редакции плана не было.

  - **`DESCRIPTION.md:13`** — «Структура меню: «Автомобили · Сервис · Запчасти · Контакты» — **четыре закреплённых пункта** в шапке на всех страницах». Пунктов пять. Записать состав с «О компании» на четвёртой позиции и назвать автора решения — заказчик, демонстрация 11.08.2026, — той же формулировкой, что в PHPDoc `SiteMenu` и в роадмапе. Три источника про меню обязаны говорить одно и то же: это уже второй раз, когда состав меняется, и первый раз показал, что расходятся они молча;
  - там же — оговорка про вкладку админки «Автосервис и запчасти» остаётся в силе (задача 3 её не трогает), либо правится, если выбрано переименование вкладки;
  - **`DESCRIPTION.md:21`** — «О компании — история и миссия, карточки сотрудников, отзывы клиентов с модерацией публикации» написано как описание существующего раздела. С этим планом оно перестаёт быть авансом; сверить формулировку с тем, что реально собрано, и дописать источник текстов (настройки `about_page.*`).

  Это не дубль задачи 7: та правит `docs/` — документацию для человека, работающего с кодом, — а `DESCRIPTION.md` живёт в `.ai-factory/` и служит входным контекстом для планирования следующих вех. Разошедшись, они дают неверную вводную не читателю, а следующему плану.

  **Логирование:** нет.

  **Файлы:** `.ai-factory/DESCRIPTION.md`
  **Зависит от:** задач 1–7

- [x] **Задача 9: веха 4.5 в роадмапе — уточнение, а не закрытие**

  Через `/aif-roadmap` (владелец файла — не этот план; скилл в проекте не установлен, поэтому правки вносятся напрямую строго по этому перечню):

  - **веху 4.5 закрытой не отмечать** (решение 10). Её вторая половина — вёрстка контактов — не сделана, и оговорка «до вехи 4.5 в публичный релиз страница не идёт» по-прежнему стоит в шаблоне `resources/views/contacts/index.blade.php:11` (в PHPDoc `ContactController` её нет — там формулировка мягче);
  - в формулировку вехи 4.5 добавить состояние: «О компании» сделана (с датой), контакты остаются заглушкой вехи 4.1 и ждут отдельного плана;
  - в разделе «Ориентиры из макета» — уточнение про меню: закреплённых пунктов стало пять, «О компании» вошла в шапку по решению заказчика от 11.08. Раздел уже содержит предупреждение, что меню «поправят обратно по частям» при сверке с макетом (`ROADMAP.md:81`), и абзац про расхождение подписей с названиями разделов (`:83`) — новая строка обязана стоять там же, иначе следующая сверка уберёт пятый пункт;
  - **в разделе «Внешние зависимости» пункт про фотографию шоу-рума уже есть** — `ROADMAP.md:129`, его внесла веха 4.9 вместе с разрешением на нейросетевые иллюстрации этапов, и он дословно разводит два случая («сгенерированное свидетельство о факте является ложным независимо от качества»). Заводить второй не нужно: **дополнить существующий** статусом задачи 2 — фотография получена и стоит на месте либо запрошена и не получена, блок остался текстовым.

  **Логирование:** нет.

  **Файлы:** `.ai-factory/ROADMAP.md`
  **Зависит от:** задач 1–8

<!-- Чекпоинт коммита: задачи 6–9 -->

## Открытые вопросы к заказчику

1. **Фотография шоу-рума** — есть ли готовая или нужна съёмка. От ответа зависит, выполняется задача 2 сейчас или переносится (решение 4). Вопрос задавать вместе с требованием к кадру: по фотографии должно быть видно, что место настоящее.
2. **Содержание страницы «О компании»** — история, миссия, состав команды. Тексты приходят из настроек, то есть заказчик может заполнить их сам, но стартовые формулировки кто-то должен написать. Ответственность заказчика по разделу 7 ТЗ, и на встрече она не поднималась.
3. **Заголовок секции заявки на странице «О компании»** — «Оставить заявку», «Познакомиться», что-то своё. Сам вопрос «нужна ли там форма» **снят при доработке плана**: кнопка «Оставить заявку» в шапке ведёт на `#lead-form` на каждой странице сайта, и страница без секции даёт мёртвую кнопку. Решать тут нечего, спрашивать стоит только формулировку.

## Риски

- **Веха 4.5 остаётся открытой, и это легко потерять.** Половина работы сделана, пункт в роадмапе не отмечен, а шаблон контактов продолжает нести оговорку про непубличную страницу. Задача 9 записывает состояние прямо в формулировку вехи именно поэтому: неотмеченный пункт без пояснения через две недели читается как «забыли сделать вообще ничего».
- **Стоковая фотография шоу-рума — соблазн, а не вариант.** Заглушки в проекте уже стоят в трёх местах, и механизм «положить файл под тем же именем» отработан. Здесь он даст ложь о существовании физического салона — и заметить её будет некому до первого приехавшего клиента. Решение 4 записано отдельным пунктом ровно потому, что технически подмена тривиальна. Роадмап уже развёл этот случай с разрешёнными нейросетевыми иллюстрациями этапов (`ROADMAP.md:129`).
- **Устаревших докблока в `SiteMenu` три, а не один, и каждый убедителен.** «Пункт меню стоит дорого» (`FOOTER`), «четыре пункта из ТЗ» (`HEADER`), «появляется в подвале вехой 4.5» (`links()`). Любой из оставшихся на месте переживёт правку константы и однажды победит — механизм ровно тот, о котором план предупреждает с первой редакции, просто мест оказалось больше.
- **Тексты про меню живут в ПЯТИ местах.** `SiteMenu`, `LayoutTest` (два списка), `docs/design-system.md`, `DESCRIPTION.md`, `ROADMAP.md`. Состав меню за этот месяц менялся дважды, и оба раза расхождение обнаруживалось не тестом, а чтением. Задачи 5, 6, 7, 8 и 9 закрывают все пять; пропуск любого из них воспроизводит ту же ситуацию.
- **Пятый пункт в шапке — предел закреплённого меню, и упирается он в вёрстку, а не в вкус.** Десктопное меню — одна строка с логотипом, телефоном и кнопкой заявки, с `whitespace-nowrap` и без права на перенос; узкое место 1024–1280px. ТЗ фиксировало четыре, макет рисовал четыре, вёрстка шапки считалась под четыре. Шестого пункта закреплённое меню не переживёт без переработки, и это стоит помнить при следующей просьбе «добавить раздел».
- **Три плана правили `home/index.blade.php` и `SiteMenu.php`, и два из них уже выполнены.** Палитра (веха 4.11) и статус «В пути» (веха 4.10) закрыты 13.08 — конкурента у этого плана больше нет, но номера строк в первой редакции он от них унаследовал устаревшими. Ориентироваться на текст и имена символов, а не на номера строк; в разделе «Исходное состояние» номера уже пересняты.
- **`SectionPagesTest` пройдёт зелёным и без `/about` — в этом и опасность.** Требование «существующие тесты без правок» здесь работает против охвата: адреса в датасетах перечислены руками, и новая страница выпадает из трёх сквозных сторожей молча. Задача 6 добавляет её явно.
- **`AboutPageContent` рискует стать «пустым файлом».** Если по ходу выяснится, что страница — это одна группа настроек и один запрос, сервис превращается в переадресацию вызова, и правильным решением будет его не заводить, как это сделано у `PartsController`. Проверять по факту, а не по плану: граница описана в `ARCHITECTURE.md` и разведена там же на живом примере.
