# ЛАО КАРС

> Сайт импорта автомобилей и автосервиса: каталог, автосервис, запчасти и единый поток заявок в админке.

Компания работает по двум направлениям — импорт автомобилей (Китай, Европа и другие страны) и автосервис (ТО, ремонт, шиномонтаж, детейлинг, подбор запчастей). Сайт закрывает обе линейки и служит основным каналом получения заявок: карточка автомобиля ведёт к заявке на конкретное авто, страница автосервиса — к записи на сервис, страница запчастей — к заявке на подбор детали, а все обращения сходятся в один список лидов.

Разделы сайта: **Каталог · Автосервис · Запчасти · Контакты** — эти четыре пункта закреплены в меню; плюс «О компании». Блога в проекте нет — решение зафиксировано в [спецификации](.ai-factory/DESCRIPTION.md).

**Текущее состояние:** собраны каркас проекта (веха 3.1), схема данных (вехи 3.2–3.3) и админка целиком — каталог, характеристики и медиабиблиотека (веха 3.4), контент, отзывы, роли и настройки сайта (веха 3.5), заявки со статусами и комментариями (веха 3.7). Публичная часть работает на бэкенде: каталог с фильтрами, пагинацией и подбором похожих (веха 3.6) — контракт GET-параметров описан в [«Каталог: фильтры и контракт URL»](docs/catalog.md); приём заявок от формы до Telegram-уведомления менеджеру (веха 3.7) — см. [«Заявки и уведомления»](docs/leads.md).

Вёрстка начата вехой 4.1: дизайн-система по UI Kit макета, шапка с закреплённым меню, подвал, карточка автомобиля и оформленная форма заявки — см. [«Дизайн-система и каркас вёрстки»](docs/design-system.md). Вехой 4.2 собрана главная: бегущая строка, первый экран с фотографией, промо-баннер, подборка авто по отметке «на главной», преимущества и двухколоночная секция заявки — редактируемые блоки и их поведение при пустых значениях описаны в [«Главной странице»](docs/homepage.md). Вехой 4.3 свёрстаны каталог и карточка автомобиля: панель фильтров поверх GET-контракта (работает и без JavaScript, Alpine добавляет только авто-сабмит), фотогалерея с деградацией без скрипта, сетка характеристик, заявка на конкретное авто, блок похожих, meta-теги из полей карточки и микроразметка `Car` + `BreadcrumbList` — см. [«Каталог: фильтры и контракт URL»](docs/catalog.md). Тогда же обнулена палитра Tailwind по умолчанию: классов вида `text-gray-400` в сборке больше нет.

Вехой 4.4 свёрстаны автосервис и запчасти. На `/services` — якорная навигация по категориям, блоки прайса из `Service` с ценой или «по запросу», оговорка о том, что цены ориентировочные, блок «почему сюда» и форма записи с **выбором конкретной услуги**: клик по строке прайса подставляет позицию в список, а без JavaScript доводит до формы, где она выбирается вручную. На `/parts` — категории запчастей карточками и форма подбора по марке, модели и VIN. Тексты блоков правятся из админки; блок без данных не рендерится вовсе. См. [«Автосервис и запчасти»](docs/services-and-parts.md).

Вехой 4.6 главная переверстана по макету v2 и выросла до четырнадцати блоков. Добавились факты первого экрана и полоса доверия, клиентский фильтр подборки по статусу, **быстрый подбор** (ползунок бюджета и тип двигателя, счётчик считает по настоящему каталогу, кнопка ведёт в каталог с уже применёнными фильтрами), этапы покупки, экосистема после покупки, витрина прайса с подстановкой позиции в форму заявки, прозрачность цены, отзывы из админки и FAQ. Промо-баннер и преимущества, которых в макете v2 нет, оставлены по решению заказчика. Контракты формы заявки и GET-параметров каталога не тронуты. См. [«Главную страницу»](docs/homepage.md).

Вехой 4.7 форма заявки отправляется без перезагрузки страницы: `fetch` лёг **поверх** обычной POST-формы, поэтому при неработающем скрипте она отправляется браузером как прежде. Успех показывает модальное окно, ошибки валидации и лимита приходят кодами и рисуются под полями. Тогда же у сотрудников появились уведомления о новой заявке тремя каналами — персональный Telegram, браузерный push и колокольчик в панели; настраиваются они в профиле сотрудника, а общий чат из окружения остался фолбэком. Ключи VAPID и HTTPS обязательны для push на проде — см. [«Уведомления сотрудникам»](docs/notifications.md).

Вехой 4.8 в панели появился раздел **«Справка»** — пятнадцать статей для сотрудника: как принять заявку, как опубликовать автомобиль, как настроить блоки главной, как включить уведомления. Тексты лежат файлами в `resources/help/`, а не в базе: справка описывает поведение кода, и её правка едет вместе с правкой кода одним коммитом. Видимость статьи спрашивается у того раздела, который она описывает, поэтому состав справки у администратора и менеджера разный, а второго списка ролей в проекте не появилось. Устройство раздела и правила текстов — в [«Справке в панели»](docs/help-center.md).

Вехой 4.10 у автомобиля появился четвёртый статус — **«В пути»**: машина уже куплена и едет к нам, в отличие от «Под заказ», где её ещё предстоит найти. Статус входит в выдачу каталога, получил свою вкладку в переключателе наличия, отдельный вид бейджа на карточке и `availability: BackOrder` в микроразметке. Вместе со статусом пересозданы три частичных индекса каталога: их предикат обязан перечислять ровно те же статусы, что и выборка доступных автомобилей, иначе индексы молча перестают браться — ни ошибки, ни красного теста, только seq scan на проде. На связку поставлен сторож в тестах, правило описано в [«Каталоге»](docs/catalog.md#правило-предикат-индекса-обязан-совпадать-со-списком-статусов-caravailable).

Адрес `/contacts` отвечает, но пока это заглушка на каркасе: содержимое приходит вехой 4.5, до неё публичная часть в релиз не идёт. См. [роадмап](.ai-factory/ROADMAP.md).

Доступ в панели разделён на администратора и менеджера: менеджер ведёт каталог и заявки, всё остальное — за администратором. Подробности — в [«Роли и доступ»](docs/admin-roles.md).

## Требования

| Инструмент | Версия | Зачем |
| :---- | :---- | :---- |
| PHP | 8.3+ (разработка ведётся на 8.5) | Приложение |
| Composer | 2.x | Зависимости PHP |
| Node.js | 20.19+ или 22.12+ (разработка ведётся на 24) | Сборка фронтенда |
| Docker | с Compose v2 | PostgreSQL и Redis |

PHP и Node запускаются локально (например, через Herd). В Docker живут только базы данных: bind mount исходников в контейнер на Windows заметно бьёт по I/O на каждом запросе. Прод-образ приложения собирается отдельно.

Расширение `phpredis` не требуется — клиентом Redis работает `predis`. Расширение `gmp` не обязательно: подпись VAPID для push-уведомлений считается через `bcmath` или на чистом PHP, а `gmp` только ускоряет её.

Браузерные push-уведомления сотрудникам требуют **HTTPS** (исключение — `localhost`) и пары ключей VAPID в `.env`. Без ключей push пропускаются с записью WARN, а заявки и Telegram работают как обычно — см. [«Уведомления сотрудникам»](docs/notifications.md).

## Быстрый старт

```bash
git clone https://github.com/leha-creator/lao-cars.git
cd lao-cars

cp .env.example .env
docker compose up -d
composer install
npm ci
php artisan key:generate
php artisan migrate
php artisan storage:link
php artisan db:seed
```

**Важно:** в `.env` укажите `REDIS_PORT=6380`, если штатный порт 6379 занят локальной службой Redis (в `.env.example` он оставлен каноническим — из этого файла собирается CI). Пятизначные порты на Windows брать не стоит: WinNAT резервирует диапазоны вида 55842–56541, и Docker падает с «socket in a way forbidden by its access permissions». Свободные диапазоны показывает `netsh interface ipv4 show excludedportrange protocol=tcp`.

`storage:link` обязателен: символическая ссылка `public/storage` в репозитории не хранится, и без неё фотографии каталога не отдаются по URL. `db:seed` наполняет базу демо-данными — без него каталог, прайс и настройки сайта пустые.

Отдельно заводить пользователя панели не нужно: `db:seed` создаёт администратора `test@example.com` и менеджера `manager@example.com`, у обоих пароль `password` (только вне production). **Штатную `php artisan make:filament-user` использовать не стоит** — роль она не спрашивает, и созданный ею пользователь окажется менеджером без доступа к настройкам. На сервере первого администратора заводит `php artisan laocars:make-admin`, см. [«Роли и доступ»](docs/admin-roles.md).

Запуск разработки — одной командой (сервер, воркер очереди, логи и Vite параллельно):

```bash
composer dev
```

Либо по отдельности:

```bash
php artisan serve        # http://localhost:8000
npm run dev              # или npm run build
php artisan queue:work   # обработка уведомлений о заявках
```

Админ-панель — по адресу `/admin`.

## Локальное окружение

`compose.yml` поднимает две службы:

| Служба | Образ | Порт на хосте |
| :---- | :---- | :---- |
| PostgreSQL | `postgres:17-alpine` | 5432 |
| Redis | `redis:7-alpine` | 6380 |

Redis смещён с 6379 намеренно: штатный порт на машине разработки занят нативной службой `redis-server`, которая поднимается заново после перезагрузки.

При первой инициализации том Postgres получает две базы: рабочую `laocars` и тестовую `laocars_testing`. Init-скрипты выполняются **только на пустом томе** — если тестовой базы не оказалось, пересоздайте окружение:

```bash
docker compose down -v
docker compose up -d
```

## Данные

| Таблица | Что хранит |
| :---- | :---- |
| `brands` | Справочник марок — источник списка для фильтра каталога |
| `cars` | Карточка автомобиля: фиксированные фильтруемые характеристики, цена, статус, SEO-поля |
| `car_photos` | Галерея карточки; главное фото — первое по `sort_order`; `thumb_path` — превью |
| `media` | Общая медиабиблиотека: переиспользуемые изображения, `path` уникален |
| `car_attributes` | Справочник динамических характеристик: машинный ключ, подпись, тип, единица, группа, порядок, флаги «в карточке» и «в фильтре» |
| `car_attribute_values` | Значения характеристик у автомобиля: одна текстовая колонка, по одному значению на пару «авто — характеристика» |
| `services` | Единая сущность услуг и запчастей, различаются категорией |
| `leads` | Заявки со всех форм с полиморфным источником (авто, услуга или общая форма) |
| `lead_comments` | Комментарии менеджеров к заявке с историей и авторством |
| `notifications` | Штатная таблица нотификаций Laravel — колокольчик в панели. Колонка `data` объявлена `jsonb`, а не `text`: Filament фильтрует по `data->>'format'`, и в PostgreSQL этот оператор к тексту неприменим |
| `push_subscriptions` | Подписки браузеров сотрудников на push; ключ — `endpoint`, у одного сотрудника их столько, сколько устройств |
| `employees`, `reviews` | Команда и отзывы для страницы «О компании»; отзывы публикуются только после модерации |
| `site_settings` | Редактируемые блоки, контакты, соцсети и SEO по умолчанию: ключ — значение (`jsonb`) с кешем в Redis |

Цена и пробег допускают `null`: это «цена по запросу» и «новый автомобиль», а не ноль.

**Динамические характеристики.** Всё, что не стало колонкой `cars` — «кузов», «цвет», «растаможен», — описывается строкой справочника `car_attributes` и хранится в `car_attribute_values` одной текстовой колонкой; тип (текст, число, «да / нет», выбор из списка) задаёт справочник и применяет на чтении. Допустимые значения типа «выбор из списка» лежат в `options` справочника, а не собираются `DISTINCT`-ом по значениям: иначе список для фильтра наполняется опечатками наполнителя. Читаются характеристики методами `Car::cardAttributes()` (сетка карточки, сгруппированная по группам справочника) и `Car::attributeValue($key)`; пишутся только через `Car::syncAttributeValues()` — там проверка значения по списку, нормализация и удаление записи при пустом значении. Оба метода чтения работают поверх загруженной связи, поэтому в списках запрос обязан идти с `with('attributeValues.attribute')`.

Машинный ключ характеристики (`body_type`, `customs_cleared`) — публичный контракт: по нему строятся GET-параметры фильтра каталога и обращения из шаблонов. Свободно правится подпись, а не ключ.

**Демо-данные.** `php artisan db:seed` создаёт 8 марок, 12 автомобилей во всех состояниях карточки (в наличии, под заказ, продан, без цены), 9 динамических характеристик с представителем каждого типа и 77 их значений по каталогу, 21 позицию прайса по всем пяти категориям, команду, отзывы (включая один на модерации) и настройки сайта из макета. Заполнение характеристик неравномерное намеренно: один автомобиль остаётся вовсе без них, чтобы карточка проверялась и на пустом наборе. Фотографии берутся из `assets/cars/` и копируются в `storage/app/public/cars/`.

Сиды идемпотентны — повторный запуск не плодит дубли. Полный сброс: `php artisan migrate:fresh --seed`. Демо-пользователь не создаётся в production.

## Тесты

```bash
php artisan test
```

Тесты идут в **реальные PostgreSQL и Redis**, а не в SQLite и array-драйверы — контейнеры должны быть подняты. Это осознанное решение: подмена драйверов скрыла бы ровно те ошибки сборки инфраструктуры, которые smoke-тесты и должны ловить. Изоляция от рабочих данных — через отдельную базу `laocars_testing` и отдельные номера баз Redis (см. `phpunit.xml`).

Копирование фотографий в тестах отключено: в `assets/cars/` 46 файлов на 128 МБ, и перекладывать их на каждом прогоне `RefreshDatabase` бессмысленно. Фото в тестах создаёт фабрика.

Если при `docker compose down` тесты продолжают проходить — значит они проверяют не то, что заявлено.

Форматирование кода:

```bash
php vendor/bin/pint          # исправить
php vendor/bin/pint --test   # только проверить
```

## CI

GitHub Actions (`.github/workflows/ci.yml`) на каждый push в `master` и каждый pull request поднимает PostgreSQL 17 и Redis 7 тех же версий, что и локально, и прогоняет: `composer install` → `npm ci` → `npm run build` → `pint --test` → `php artisan test`.

Статический анализ в базовый CI пока не входит.

## Стек

- **Backend:** PHP 8.3+, Laravel 13, PostgreSQL 17, Eloquent
- **Админ-панель:** Filament 5 на Livewire 4
- **Frontend:** Blade + Alpine.js 3 + Tailwind CSS 4, сборка Vite
- **Очереди и кеш:** Redis 7 (`predis`)
- **Тесты:** Pest 4

Серверный рендер выбран ради SEO каталога: списки и карточки должны индексироваться, а фильтры — оставаться обычными GET-параметрами. Alpine отвечает только за локальную интерактивность.

Токены дизайн-системы живут в `@theme` файла `resources/css/app.css` — Tailwind v4 конфигурируется в CSS, файла `tailwind.config.js` в проекте быть не должно. Палитра Tailwind по умолчанию обнулена (`--color-*: initial`), вручную возвращены только `white` и `black`: на них держатся все обводки проекта. Шрифты Unbounded и Manrope самохостятся: `npm run build` скачивает woff2 и отдаёт их со своего домена, подключения к Google Fonts в рантайме нет. Подробности — в [«Дизайн-система и каркас вёрстки»](docs/design-system.md).

## Документация

| Документ | Путь | Описание |
| :---- | :---- | :---- |
| Дизайн-система и каркас | [docs/design-system.md](docs/design-system.md) | Токены, шрифты, компоненты каркаса, как добавить страницу, диагностика вёрстки |
| Главная страница | [docs/homepage.md](docs/homepage.md) | Блоки главной, редактируемые настройки, подборка авто и её лимит, быстрый подбор, витрина прайса, отзывы, замена фоновых фото |
| Каталог: фильтры и URL | [docs/catalog.md](docs/catalog.md) | GET-параметры каталога, сортировки, индексация, панель фильтров без JS, галерея, микроразметка карточки |
| Автосервис и запчасти | [docs/services-and-parts.md](docs/services-and-parts.md) | Блоки прайса и откуда они берутся, поля админки, выбор услуги в форме, поведение без JavaScript |
| Заявки и уведомления | [docs/leads.md](docs/leads.md) | Путь заявки, два формата ответа формы, защита форм, настройка бота, воркер, работа менеджера |
| Уведомления сотрудникам | [docs/notifications.md](docs/notifications.md) | Три канала доставки, кто получатель, настройка в кабинете, ключи VAPID и HTTPS, диагностика |
| Админка каталога | [docs/admin-catalog.md](docs/admin-catalog.md) | Наполнение каталога: марки, карточки, фото, характеристики, медиабиблиотека |
| Роли и доступ | [docs/admin-roles.md](docs/admin-roles.md) | Права администратора и менеджера, заведение сотрудников, первый запуск на проде |
| Контент и настройки | [docs/admin-content.md](docs/admin-content.md) | Услуги и запчасти, команда, модерация отзывов, настройки сайта |
| Справка в панели | [docs/help-center.md](docs/help-center.md) | Устройство раздела справки, как добавить статью, видимость по ролям, что запрещено в текстах |
| Техническое задание | [ТЗ_ЛАО_КАРС.md](ТЗ_ЛАО_КАРС.md) | Первоисточник требований заказчика |
| Спецификация | [.ai-factory/DESCRIPTION.md](.ai-factory/DESCRIPTION.md) | Стек, принятые решения, границы MVP |
| Архитектура | [.ai-factory/ARCHITECTURE.md](.ai-factory/ARCHITECTURE.md) | Слои, правила зависимостей, антипаттерны |
| Роадмап | [.ai-factory/ROADMAP.md](.ai-factory/ROADMAP.md) | Вехи по этапам и что уже закрыто |
| Конвенции кода | [.ai-factory/rules/base.md](.ai-factory/rules/base.md) | Именование, структура, ошибки, логирование |
| Карта проекта | [AGENTS.md](AGENTS.md) | Структура репозитория и правила для агентов |
